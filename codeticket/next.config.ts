/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/nextjs',
  images: {
    domains: ['rio.websummit.com', 'outro.dominio.com'], // adicione outros domínios conforme necessário
  },
};

export default nextConfig;
